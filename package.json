{
  "name": "todo-app-deploy",
  "version": "1.0.0",
  "description": "Deployment configuration for Todo App",
  "scripts": {
    "start": "node server.js",
    "build": "node generate-config.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "sirv-cli": "^2.0.2"
  }
}
